/**
 * AMR Types (JSDoc for editors; runtime is plain CJS)
 * @typedef {('medium'|'high')} ReasoningLevel
 * @typedef {('L0'|'L1'|'H')} TaskClass
 * @typedef {{ switch?: ReasoningLevel, reason?: string }} RouterDecision
 */

module.exports = {};

