#!/usr/bin/env bash
set -euo pipefail
ROOT="$(pwd)"
SCRIPT=".super-prompt/utils/init/init_sp.py"
if [[ ! -f "$SCRIPT" ]]; then
  echo "-------- sp-reinit: cannot find $SCRIPT in current project" >&2
  exit 1
fi
exec python3 "$SCRIPT" --mode reinit --project-root "$ROOT"

